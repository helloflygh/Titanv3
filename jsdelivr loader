<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Ultimate Game Stash - Loader</title>

<style>
    html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        background: #000;
    }

    /* THE GAME AREA */
    #game-container {
        width: 100%;
        height: 100vh;
        overflow: auto; 
        position: relative;
        z-index: 1;
    }

    /* --- Centered Panic Button --- */
    .panic-btn {
        position: fixed; 
        top: 10px;        /* Distance from top */
        left: 50%;       /* Move to horizontal center */
        transform: translateX(-50%); /* Offset by half its width to perfectly center */
        
        font-size: 22px; 
        padding: 12px 45px; 
        color: #000000 !important; 
        background: #ff3333; 
        font-family: 'Arial Black', sans-serif;
        cursor: pointer; 
        z-index: 99999; 
        border-radius: 10px;
        font-weight: 900; 
        border: 4px solid #000;
        box-shadow: 0 5px 15px rgba(0,0,0,0.6);
        transition: all 0.2s ease-in-out;
        text-transform: uppercase;
        letter-spacing: 2px;
    }

    .panic-btn:hover { 
        transform: translateX(-50%) scale(1.05); 
        background: #ff4d4d;
    }

    .panic-btn:active {
        transform: translateX(-50%) scale(0.95);
    }

    /* --- Panic Overlay --- */
    #panic-overlay { 
        display: none; 
        position: fixed; 
        top: 0; left: 0; 
        width: 100vw; height: 100vh; 
        background: #fff; 
        z-index: 100000; 
    }
    #panic-iframe { width: 100%; height: 100%; border: none; }
    
    .close-panic {
        position: absolute; top: 15px; left: 15px; 
        font-size: 30px; cursor: pointer; color: #000;
        font-weight: bold;
        z-index: 100001;
        opacity: 0.05; /* Barely visible for stealth */
    }
</style>
</head>
<body>

<button class="panic-btn" onclick="doPanic()">PANIC</button>

<div id="game-container">
    <div style="color: white; text-align: center; padding-top: 150px;">
        <h1>Game Stash Area</h1>
        <p>Replace this text with your game's HTML/JS code.</p>
    </div>

    </div>

<div id="panic-overlay">
    <span class="close-panic" onclick="closePanic()">&times;</span>
    <iframe id="panic-iframe"></iframe>
</div>

<script>
const PANIC_URL = "https://clever.com"; 

function doPanic() {
    document.getElementById("panic-iframe").src = PANIC_URL;
    document.getElementById("panic-overlay").style.display = "block";
}

function closePanic() {
    document.getElementById("panic-overlay").style.display = "none";
    document.getElementById("panic-iframe").src = "";
}

window.addEventListener('keydown', (e) => {
    if (e.ctrlKey && e.key.toLowerCase() === 'x') {
        e.preventDefault(); 
        doPanic();
    }
});
</script>

</body>
</html>
